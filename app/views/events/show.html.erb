


<%= render 'details' %>

<p>
 <h4 class="nerd"> Location Votes:</h4>
  <% @location_votes.each do |info| %>
   <h5 class="nerd"><%= Location.find(info[:id]).name %> (<%= info[:count] %>)</h5>
  <% end %>
<br>
 
  <h4 class="nerd">Event Time Votes:</h4>
  <% @event_time_votes.each do |info| %>
   <h5 class="nerd"><%= EventTime.find(info[:id]).time_description.strftime('%l:%M %p') %> (<%= info[:count] %>) </h5>
  <% end %>

</p>

<h4>Send the link below to have others vote on this event</h4>

<h4>http://letdo.herokuapp.com<%=new_vote_path(event_id: @event.id) %></h4>

<br>
 
 
<!-- link_to 'Edit Your Event', edit_event_url,  class: 'btn btn-primary'  -->
